<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse">    
      <ul class="nav navbar-nav navbar-left">
        <li>{{#link-to 'productos.new'}}Nuevo Producto{{/link-to}}</li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<div class="row">

  <div class="col-sm">
    <table class="table">
      <thead>
        <tr>
          <th>CODIGO</th>
          <th>DESCRIPCION</th>
          <th>PRECIO VENTA</th>
          <th>ACCIONES</th>
        </tr>
      </thead>
      <tbody>
        {{#each model as |item|}}
        <tr>
          <td>{{item.codigo}}</td>
          <td>{{item.texto}}</td>
          <td>{{formatCurrency item.precioVenta}}</td>
          <td>
            {{#link-to 'productos.edit' item.id classNames="btn btn-default btn-xs"}}Modificar{{/link-to}}
            <button class="btn btn-default btn-xs" {{action 'delete' item}}>Eliminar</button>
          </td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>  
</div>

{{outlet}}

<div id="confirmDeleteModal" class="modal fade" tabindex="-1" role="dialog">
  <div class="modal-dialog">
    <div class="modal-content panel-info">
      <div class="modal-header alert-info">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title">Confirmar Eliminación</h4>
      </div>
      <div class="modal-body">
      Esta seguro que desea eliminar el producto seleccionado?
    </div>
    <div class="modal-footer">
      <a href="#" class="btn btn-info" role="button" {{action "confirmDelete"}}>Eliminar</a>
      <a href="javascript:void(0)" class="btn btn-default" role="button" onclick="$('#confirmDeleteModal').modal('hide');">Cancelar</a>
    </div>
  </div><!-- /.modal-content -->
</div><!-- /.modal-dialog -->
</div><!-- /.modal -->