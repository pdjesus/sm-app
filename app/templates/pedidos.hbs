<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse">    
      <ul class="nav navbar-nav navbar-left">
        <li>{{#link-to 'pedidos.new'}}Registrar Pedido{{/link-to}}</li>
        <li>{{#link-to 'pedidos'}}Agregar Anotación{{/link-to}}</li>
      </ul>
      <form class="navbar-form navbar-right" role="search">
        <div class="form-group">
          {{#power-select options=estados selected=estadoSelected placeholder="Seleccionar" onchange=(action "setEstado") as |estado|}}
          {{estado}}
          {{/power-select}}      
        </div>
      </form>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<div class="row">

  <div class="col-sm">
    <table class="table">
      <thead>
        <tr>
          <th>FECHA PEDIDO</th>
          <th>CLIENTE</th>
          <th>DNI</th>
          <th>PRODUCTO</th>
          <th>TALLE</th>
          <th>CANTIDAD</th>
          <th>ENTREGA</th>
          <th>ESTADO</th>
        </tr>
      </thead>
      <tbody>
        {{#each filteredModel as |item|}}
        <tr>
          <td>{{moment-format item.fechaPedido}}</td>
          <td>{{item.ficha.cliente.nombre}}</td>
          <td>{{item.ficha.cliente.dni}}</td>
          <td>{{item.producto.texto}}</td>
          <td>{{item.talle.talle}}</td>
          <td>{{item.cantidad}}</td>
          <td>{{format-currency item.entrega}}</td>
          <td>{{item.estado}}</td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</div>