import DS from 'ember-data';

export default DS.JSONAPIAdapter.extend({

  // shouldReloadAll(store, snapshotRecordArray) { return true; },

  // shouldReloadRecord(store, snapshotRecordArray) { return true; }
  
});
